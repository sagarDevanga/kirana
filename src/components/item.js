import React from 'react'

const item = (props)=>{

    return (
        <div style={{backgroundColor:"red"}}>
            <h1>PRODUCT NAME</h1>
            <button>Add to Cart</button>
        </div>

    )
}

export default item