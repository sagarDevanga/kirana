import React from 'react'

const register = (props) => {

    return (

        <div>
            <input type="email" />
            <button>REGISTER</button>
        </div>
    )
}

export default register